<%= nested_form_for(@ticket) do |f| %>
  <% if @ticket.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket.errors.count, "error") %> prohibited this ticket from being saved:</h2>

      <ul>
      <% @ticket.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status_id %><br>
    <%= f.text_field :status_id %>
  </div>

  <%= f.fields_for :customer do |ff| %>
      <div>
        <%= ff.label :customer_name %>
        <%= ff.text_field :name %>
        <%= ff.label :customer_phone %>
        <%= ff.text_field :phone %>
        <%= ff.label :customer_email %>
        <%= ff.text_field :email %>
      </div>
  <% end %>
    
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
